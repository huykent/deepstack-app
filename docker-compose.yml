version: '3.8'

services:
  deepstack:
    image: deepquestai/deepstack
    container_name: deepstack-ai
    ports:
      - "15679:15679"
    environment:
      VISION-FACE: 'True'
      VISION-DETECTION: 'True'
      VISION-PLATE: 'True'
      VISION-OCR: 'True'
    restart: unless-stopped

  nodeapp:
    build: ./app
    container_name: node-webapp
    ports:
      - "15678:15678"
    depends_on:
      - deepstack
    environment:
      DEEPSTACK_URL: "http://deepstack:15679"
      PASSWORD: "9999"

    volumes:
      - ./app:/usr/src/app
    restart: unless-stopped
